# Chunks Directory (`data/chunks/`)

Contains text chunks generated by fixed/semantic segmentation with metadata attributes.

The repository also keeps a small `bootstrap.jsonl` fixture here so a clean local checkout
can answer demo questions without needing a full external corpus on day one.
