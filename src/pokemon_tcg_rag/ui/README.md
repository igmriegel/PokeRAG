# Streamlit UI Module (`ui/`)

This directory contains the Streamlit web application:
- `streamlit_app.py`: Interactive user interface allowing users to submit queries, view rewritten queries, inspect retrieved context chunks and source citations, view execution latency metrics, and submit positive (+1) or negative (-1) feedback.
