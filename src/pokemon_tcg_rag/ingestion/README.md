# Ingestion Module (`ingestion/`)

This directory contains data pipeline modules responsible for automated web crawling, PDF extraction via PyMuPDF/PyMuPDF4LLM, HTML parsing, text normalization, and metadata-enriched chunking.
