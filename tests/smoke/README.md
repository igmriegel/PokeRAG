# Smoke Tests Directory (`tests/smoke/`)

Contains quick smoke tests designed for CI validation and deployment verification.
Checks basic container startup, database connections, and health endpoints.
